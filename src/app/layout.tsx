// Root layout — minimal shell required by Next.js App Router.
// All HEC-specific providers and fonts live in [locale]/layout.tsx.
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
